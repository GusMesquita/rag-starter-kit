ANTHROPIC_API_KEY=
CHROMA_PATH=./chroma_data
COLLECTION_NAME=documents
TOP_K=4
# Comma-separated shared secrets for the X-API-Key header. Leave empty only
# for local dev — never deploy with this unset.
API_KEYS=
